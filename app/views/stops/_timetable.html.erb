<table class="table">
  <tr>
    <th class="text-center">時</th>
    <th class="text-center">分</th>
  </tr>
  <% (0..24).each do |h| %>
    <tr>
      <th class="text-center"><%= "%02d" % h %></th>
      <% busmin = [] %>
      <% Bus.order(id: :asc).each do |bus| %>
        <% bustime = bus.plans.find_by(stop_id: stop.id).dept_time %>
        <% if bustime && bustime.to_time.strftime("%H") == "%02d" % h %>
          <% busmin.push(bustime.to_time.strftime("%M")) %>
        <% end %>
      <% end %>
      <% busmin.sort!.each do |m| %>
        <td class="text-center"><%= m %></td>
      <% end %>
    </tr>
  <% end %>
</table>
