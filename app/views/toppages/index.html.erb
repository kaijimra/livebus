<% if logged_in? %>
  <div class="row">
    <div class="col-sm-6">
      <h2 class="text-start text-sm-center">バス運行状況</h2>
    </div>
    <div class="col-sm-6 ">
      <h3 class="text-start text-sm-center">現在時刻 <%= @simtime %></h3>
    </div>
  </div>

  <div class="row">
    <div class="mx-auto col-md-8">
      <%= render 'toppages/realtime_bus', stops: @stops %>
    </div>
  </div>
<% end %>